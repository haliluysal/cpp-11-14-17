
/*

* points to another type
* holds the memory address of another variable
* used for indirect access to other variables
* does "not" have be initialized
* declared wit * operator
*		int *p1;
*		int *p2, *p3, p4 // p4 is normal integer not pointer
* 
* & is the address of operator
* dereference operator "*"
*		int x = 10;
*		int *ptr = &x;
*		*ptr = 5;
*		int y = *ptr;
* 
* DO NOT try to write to or read from nullptr

*/