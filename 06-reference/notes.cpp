/*

* always needs an initializer
* it is bound to its referent
* it can be used to modify a variable indirectly
* a reference is NOT a new variable just another name! - no memory is allocated for reference
* 
* 
* reference								pointer
* always needs an initlializer			initializer is optional
* initializer should be l-value			initializer need not to be l-value
* canNOT be nullptr						can be nullptr
* bound to its referent					can point to other variables


*/